# Same Neon Postgres instance as every other service — this service is a
# read-only reporting layer, it never writes. Needs SELECT access to
# calls / call_analysis / assignments / recommendations / users.
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require&channel_binding=require
